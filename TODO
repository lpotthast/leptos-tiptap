# TODO

- Decide whether to add non-focusing variants/options for extension convenience commands that currently call `focus()`.
- Switch component from rendering <leptos-tiptap-instance> to <div class="leptos-tiptap-instance">. Use leptos-classes and leptos-styles.
- Generate or centralize extension metadata so Cargo features, Rust registration, Rust command protocol, and TypeScript command registration can not drift.
- Revisit whether `TiptapSelectionState` should stay a flat bool struct or move to an extensible keyed model before adding many more extensions.
