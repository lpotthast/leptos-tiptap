[package]
name = "demo-csr"
version = "0.5.0"
edition = "2024"

[profile.release]
opt-level = "z"
lto = "thin"
debug = 0

[dependencies]
console_error_panic_hook = "0.1.7"
demo-app = { path = "../demo-app", features = ["csr"] }
leptos = { version = "0.8.2", features = ["csr"] }
